server:
  port: 8764

spring:
  application:
    name: service-ribbon

feign:
  hystrix:
    enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/


logging:
  config: classpath:logback-debug.xml